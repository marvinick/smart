<%= link_to "Enter Budget", new_budget_path %>
<%= link_to "Time Table", new_timetable_path %>

<% @budgets.each do |budget| %>
	<%= budget.amount %>
<% end %>

<% @timetables.each do |t| %>
	<%= t.day %>
	<%= t.hour %>
<% end %>